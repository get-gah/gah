name: 🧪 Unit Tests

on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-slim
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Test
        shell: bash
        run: ./test/bats/bin/bats ./test

